'use strict';
const { randomUUID } = require('node:crypto');
const { withTransaction } = require('../../core/database/sqlite-database');
const { writeAudit } = require('../../core/audit-log');
const { roundQuantity } = require('../inventory/inventory-rules');

function createInventoryLogisticsService({db,inventory,now=()=>new Date().toISOString(),idFactory=p=>`${p}-${randomUUID()}`}={}){
  if(!db||!inventory)throw new TypeError('db and inventory are required.');
  const norm=v=>String(v||'').trim();
  const loc=v=>norm(v)||'MAIN';
  const requireManager=actor=>{if(!['manager','admin'].includes(String(actor?.role||'')))throw new Error('Autorizacao de gerente necessaria para operacao logistica.');};
  const requireLocation=id=>{const value=loc(id);if(!db.prepare('SELECT id FROM stock_locations WHERE id=? AND active=1').get(value))throw new Error(`Local de estoque ${value} nao encontrado ou inativo.`);return value;};

  function listLocations({includeInactive=false}={}){return db.prepare(`SELECT id,name,type,active,created_at AS createdAt,updated_at AS updatedAt FROM stock_locations${includeInactive?'':' WHERE active=1'} ORDER BY name,id`).all().map(r=>({...r,active:Boolean(r.active)}));}
  function createLocation(input={},actor=null){requireManager(actor);const id=norm(input.id||idFactory('loc'));const name=norm(input.name);const type=norm(input.type||'OTHER').toUpperCase();if(!name)throw new Error('Nome do local de estoque obrigatorio.');if(!['STORE','WAREHOUSE','INTERNAL','OTHER'].includes(type))throw new Error('Tipo de local de estoque invalido.');const ts=now();db.prepare('INSERT INTO stock_locations(id,name,type,active,created_at,updated_at) VALUES(?,?,?,?,?,?)').run(id,name,type,input.active===false?0:1,ts,ts);writeAudit(db,{action:'inventory.location.create',entity:'stock-location',entityId:id,actor,context:{name,type}},now);return listLocations({includeInactive:true}).find(x=>x.id===id);}

  function getAvailability(productId,locationId='MAIN',{excludeSourceType=null,excludeSourceId=null}={}){
    const physicalQuantity=inventory.getBalance(productId,{locationId:loc(locationId)});
    const clauses=["product_id=?","location_id=?","status='ACTIVE'"];const params=[String(productId),loc(locationId)];
    if(excludeSourceType&&excludeSourceId){clauses.push('NOT (source_type=? AND source_id=?)');params.push(String(excludeSourceType),String(excludeSourceId));}
    const row=db.prepare(`SELECT COALESCE(SUM(quantity-consumed_quantity),0) AS q FROM inventory_reservations WHERE ${clauses.join(' AND ')}`).get(...params);
    const reservedQuantity=roundQuantity(Number(row?.q||0));
    return{physicalQuantity:roundQuantity(physicalQuantity),reservedQuantity,availableQuantity:roundQuantity(physicalQuantity-reservedQuantity)};
  }

  function mapReservation(row){return row&&{id:row.id,productId:row.product_id,locationId:row.location_id,quantity:roundQuantity(row.quantity),consumedQuantity:roundQuantity(row.consumed_quantity),remainingQuantity:roundQuantity(row.quantity-row.consumed_quantity),sourceType:row.source_type,sourceId:row.source_id,boundSaleId:row.bound_sale_id,status:row.status,createdAt:row.created_at,updatedAt:row.updated_at};}
  function createReservation(input={},actor=null){
    return withTransaction(db,()=>{
      const productId=norm(input.productId),locationId=requireLocation(input.locationId),sourceType=norm(input.sourceType),sourceId=norm(input.sourceId),quantity=roundQuantity(input.quantity);
      if(!productId||!sourceType||!sourceId||quantity<=0)throw new Error('Reserva de estoque invalida.');
      const existing=db.prepare('SELECT * FROM inventory_reservations WHERE source_type=? AND source_id=? AND product_id=? AND location_id=?').get(sourceType,sourceId,productId,locationId);
      if(existing){if(existing.status==='ACTIVE'&&roundQuantity(existing.quantity-existing.consumed_quantity)===quantity)return mapReservation(existing);throw new Error('Ja existe reserva para esta origem/produto/local.');}
      const availability=getAvailability(productId,locationId);
      if(quantity>availability.availableQuantity)throw new Error(`Estoque disponivel insuficiente. Disponivel: ${availability.availableQuantity}.`);
      const id=norm(input.id||idFactory('reserve'));const ts=now();
      db.prepare(`INSERT INTO inventory_reservations(id,product_id,location_id,quantity,consumed_quantity,source_type,source_id,status,created_at,updated_at) VALUES(?,?,?,?,0,?,?, 'ACTIVE',?,?)`).run(id,productId,locationId,quantity,sourceType,sourceId,ts,ts);
      writeAudit(db,{action:'inventory.reserve',entity:'inventory-reservation',entityId:id,actor,context:{productId,locationId,quantity,sourceType,sourceId}},now);
      return mapReservation(db.prepare('SELECT * FROM inventory_reservations WHERE id=?').get(id));
    });
  }
  function listReservations(filters={}){const clauses=[];const params=[];if(filters.status){clauses.push('status=?');params.push(String(filters.status).toUpperCase());}if(filters.sourceType){clauses.push('source_type=?');params.push(String(filters.sourceType));}if(filters.sourceId){clauses.push('source_id=?');params.push(String(filters.sourceId));}if(filters.locationId){clauses.push('location_id=?');params.push(loc(filters.locationId));}return db.prepare(`SELECT * FROM inventory_reservations${clauses.length?` WHERE ${clauses.join(' AND ')}`:''} ORDER BY created_at,id`).all(...params).map(mapReservation);}
  function releaseSourceReservations(sourceType,sourceId,actor=null){const ts=now();const info=db.prepare("UPDATE inventory_reservations SET status='RELEASED',released_at=?,updated_at=? WHERE source_type=? AND source_id=? AND status='ACTIVE'").run(ts,ts,String(sourceType),String(sourceId));if(info.changes)writeAudit(db,{action:'inventory.reservations.release',entity:'inventory-reservation-source',entityId:`${sourceType}:${sourceId}`,actor,context:{count:info.changes}},now);return info.changes;}
  function bindSourceReservationsToSale(sourceType,sourceId,saleId){const info=db.prepare("UPDATE inventory_reservations SET bound_sale_id=?,updated_at=? WHERE source_type=? AND source_id=? AND status='ACTIVE'").run(String(saleId),now(),String(sourceType),String(sourceId));return info.changes;}
  function consumeSaleReservations(saleId){
    return withTransaction(db,()=>{
      const sale=db.prepare('SELECT stock_location_id AS locationId FROM sales WHERE id=?').get(String(saleId));if(!sale)return 0;
      const items=db.prepare('SELECT product_id AS productId,quantity FROM sale_items WHERE sale_id=?').all(String(saleId));
      let changed=0;
      for(const item of items){let remaining=roundQuantity(item.quantity);const rows=db.prepare("SELECT * FROM inventory_reservations WHERE bound_sale_id=? AND product_id=? AND location_id=? AND status='ACTIVE' ORDER BY created_at,id").all(String(saleId),String(item.productId),loc(sale.locationId));for(const row of rows){if(remaining<=0)break;const available=roundQuantity(row.quantity-row.consumed_quantity);const take=Math.min(available,remaining);const consumed=roundQuantity(row.consumed_quantity+take);const status=consumed>=roundQuantity(row.quantity)?'CONSUMED':'ACTIVE';db.prepare('UPDATE inventory_reservations SET consumed_quantity=?,status=?,consumed_at=?,updated_at=? WHERE id=?').run(consumed,status,status==='CONSUMED'?now():null,now(),row.id);remaining=roundQuantity(remaining-take);changed++;}}
      return changed;
    });
  }

  function mapTransfer(row){if(!row)return null;const items=db.prepare('SELECT id,product_id AS productId,quantity,created_at AS createdAt FROM stock_transfer_items WHERE transfer_id=? ORDER BY created_at,id').all(row.id).map(item=>({...item,quantity:roundQuantity(item.quantity)}));return{id:row.id,fromLocationId:row.from_location_id,toLocationId:row.to_location_id,status:row.status,dispatchIdempotencyKey:row.dispatch_idempotency_key,receiveIdempotencyKey:row.receive_idempotency_key,createdBy:row.created_by,createdAt:row.created_at,updatedAt:row.updated_at,dispatchedAt:row.dispatched_at,receivedAt:row.received_at,cancelledAt:row.cancelled_at,cancelReason:row.cancel_reason,items};}
  function getTransfer(id){return mapTransfer(db.prepare('SELECT * FROM stock_transfers WHERE id=?').get(String(id)));}
  function requireTransfer(id){const row=db.prepare('SELECT * FROM stock_transfers WHERE id=?').get(String(id));if(!row)throw new Error('Transferencia nao encontrada.');return row;}
  function listTransfers(filters={}){const clauses=[];const params=[];if(filters.status){clauses.push('status=?');params.push(String(filters.status).toUpperCase());}if(filters.locationId){clauses.push('(from_location_id=? OR to_location_id=?)');params.push(loc(filters.locationId),loc(filters.locationId));}return db.prepare(`SELECT * FROM stock_transfers${clauses.length?` WHERE ${clauses.join(' AND ')}`:''} ORDER BY created_at DESC,id DESC`).all(...params).map(mapTransfer);}
  function createTransfer(input={},actor=null){requireManager(actor);const fromLocationId=requireLocation(input.fromLocationId),toLocationId=requireLocation(input.toLocationId);if(fromLocationId===toLocationId)throw new Error('Origem e destino da transferencia devem ser diferentes.');if(!Array.isArray(input.items)||!input.items.length)throw new Error('Transferencia deve possuir ao menos um item.');const seen=new Set();const items=input.items.map(item=>{const productId=norm(item.productId);if(seen.has(productId))throw new Error('Produto duplicado na transferencia.');seen.add(productId);if(!db.prepare('SELECT id FROM products WHERE id=? AND active=1').get(productId))throw new Error(`Produto ${productId} nao encontrado ou inativo.`);const quantity=roundQuantity(item.quantity);if(quantity<=0)throw new Error('Quantidade da transferencia deve ser maior que zero.');return{productId,quantity};});const id=norm(input.id||idFactory('transfer')),ts=now();return withTransaction(db,()=>{db.prepare(`INSERT INTO stock_transfers(id,from_location_id,to_location_id,status,created_by,created_at,updated_at) VALUES(?,?,?,'DRAFT',?,?,?)`).run(id,fromLocationId,toLocationId,actor?.userId||null,ts,ts);const insert=db.prepare('INSERT INTO stock_transfer_items(id,transfer_id,product_id,quantity,created_at) VALUES(?,?,?,?,?)');for(const item of items)insert.run(idFactory('tri'),id,item.productId,item.quantity,ts);writeAudit(db,{action:'inventory.transfer.create',entity:'stock-transfer',entityId:id,actor,context:{fromLocationId,toLocationId,itemCount:items.length}},now);return getTransfer(id);});}
  function dispatchTransfer(id,{idempotencyKey}={},actor=null){requireManager(actor);const key=norm(idempotencyKey);if(!key)throw new Error('Chave de idempotencia obrigatoria no despacho.');return withTransaction(db,()=>{const row=requireTransfer(id);if(row.dispatch_idempotency_key===key&&['IN_TRANSIT','RECEIVED','CANCELLED'].includes(row.status))return getTransfer(id);if(row.status!=='DRAFT')throw new Error(`Transferencia nao pode ser despachada no status ${row.status}.`);const transfer=getTransfer(id);for(const item of transfer.items){const available=getAvailability(item.productId,row.from_location_id);if(item.quantity>available.availableQuantity)throw new Error(`Estoque disponivel insuficiente para transferir ${item.productId}. Disponivel: ${available.availableQuantity}.`);}const ts=now();for(const item of transfer.items)inventory.move({productId:item.productId,locationId:row.from_location_id,type:'adjustment-out',quantityDelta:-item.quantity,reason:`Transferencia ${row.id} em transito`,sourceType:'stock-transfer',sourceId:row.id},actor);db.prepare("UPDATE stock_transfers SET status='IN_TRANSIT',dispatch_idempotency_key=?,dispatched_at=?,updated_at=? WHERE id=?").run(key,ts,ts,row.id);writeAudit(db,{action:'inventory.transfer.dispatch',entity:'stock-transfer',entityId:row.id,actor,context:{key}},now);return getTransfer(row.id);});}
  function receiveTransfer(id,{idempotencyKey}={},actor=null){requireManager(actor);const key=norm(idempotencyKey);if(!key)throw new Error('Chave de idempotencia obrigatoria no recebimento da transferencia.');return withTransaction(db,()=>{const row=requireTransfer(id);if(row.receive_idempotency_key===key&&row.status==='RECEIVED')return getTransfer(id);if(row.status!=='IN_TRANSIT')throw new Error(`Transferencia nao pode ser recebida no status ${row.status}.`);const transfer=getTransfer(id),ts=now();for(const item of transfer.items)inventory.move({productId:item.productId,locationId:row.to_location_id,type:'adjustment-in',quantityDelta:item.quantity,reason:`Recebimento transferencia ${row.id}`,sourceType:'stock-transfer',sourceId:row.id},actor);db.prepare("UPDATE stock_transfers SET status='RECEIVED',receive_idempotency_key=?,received_at=?,updated_at=? WHERE id=?").run(key,ts,ts,row.id);writeAudit(db,{action:'inventory.transfer.receive',entity:'stock-transfer',entityId:row.id,actor,context:{key}},now);return getTransfer(row.id);});}
  function cancelTransfer(id,{reason='' }={},actor=null){requireManager(actor);const text=norm(reason);if(!text)throw new Error('Informe o motivo do cancelamento da transferencia.');return withTransaction(db,()=>{const row=requireTransfer(id);if(row.status==='CANCELLED')return getTransfer(id);if(row.status==='RECEIVED')throw new Error('Transferencia recebida nao pode ser cancelada.');const transfer=getTransfer(id);if(row.status==='IN_TRANSIT'){for(const item of transfer.items)inventory.move({productId:item.productId,locationId:row.from_location_id,type:'adjustment-in',quantityDelta:item.quantity,reason:`Cancelamento transferencia ${row.id}`,sourceType:'stock-transfer-cancel',sourceId:row.id},actor);}const ts=now();db.prepare("UPDATE stock_transfers SET status='CANCELLED',cancelled_at=?,cancel_reason=?,updated_at=? WHERE id=?").run(ts,text,ts,row.id);writeAudit(db,{action:'inventory.transfer.cancel',entity:'stock-transfer',entityId:row.id,actor,context:{reason:text}},now);return getTransfer(row.id);});}

  return{listLocations,createLocation,getAvailability,createReservation,listReservations,releaseSourceReservations,bindSourceReservationsToSale,consumeSaleReservations,createTransfer,getTransfer,listTransfers,dispatchTransfer,receiveTransfer,cancelTransfer};
}
module.exports={createInventoryLogisticsService};

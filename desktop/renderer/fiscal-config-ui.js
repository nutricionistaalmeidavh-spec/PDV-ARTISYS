'use strict';
(()=>{
 const root=window,content=document.getElementById('route-content');if(!content||!root.FiscalConfigApi)return;
 const {FiscalConfigClient}=root.FiscalConfigApi,api=new FiscalConfigClient();let mounted=null,tab='company',state={settings:null,profiles:[],products:[],coverage:null,sequence:null,readiness:null};
 const esc=v=>String(v??'').replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'})[c]);
 const toast=(m,t='')=>{const h=document.getElementById('toast-root');if(!h)return;const e=document.createElement('div');e.className='toast '+t;e.textContent=m;h.appendChild(e);setTimeout(()=>e.remove(),3500);};
 const field=(l,n,v='',a='')=>'<label class="fiscal-field"><span>'+esc(l)+'</span><input name="'+esc(n)+'" value="'+esc(v??'')+'" '+a+' /></label>';
 const select=(l,n,v,o)=>'<label class="fiscal-field"><span>'+esc(l)+'</span><select name="'+esc(n)+'">'+o.map(x=>'<option value="'+esc(x[0])+'" '+(String(v)===String(x[0])?'selected':'')+'>'+esc(x[1])+'</option>').join('')+'</select></label>';
 async function load(){await api.initialize();const r=await Promise.all([api.settings().catch(()=>null),api.profiles(true).catch(()=>[]),api.products().catch(()=>[]),api.coverage().catch(()=>null),api.readiness().catch(()=>null)]);state={...state,settings:r[0],profiles:Array.isArray(r[1])?r[1]:[],products:Array.isArray(r[2])?r[2]:[],coverage:r[3],readiness:r[4]};const s=state.settings;if(s?.documentType&&s?.environment&&s?.series)state.sequence=await api.sequence({documentType:s.documentType,environment:s.environment,series:s.series}).catch(()=>null);}
 function company(){const s=state.settings||{},a=s.address||{};return '<form id="fiscal-company-form" class="fiscal-form" data-phase="P2"><div class="fiscal-section-head"><div><h3>P2 · Dados fiscais da empresa</h3><p>Configuração canônica do emitente. Certificado e segredos continuam fora do renderer.</p></div></div><div class="fiscal-grid">'+select('Provedor','provider',s.provider||'acbr-local',[['acbr-local','ACBr local']])+select('Documento','documentType',s.documentType||'nfce',[['nfce','NFC-e'],['nfe','NF-e']])+select('Ambiente','environment',s.environment||'homologation',[['homologation','Homologação'],['production','Produção']])+field('CNPJ','cnpj',s.cnpj,'maxlength="18"')+field('Inscrição estadual','stateRegistration',s.stateRegistration)+field('Razão social','legalName',s.legalName)+field('Nome fantasia','tradeName',s.tradeName)+select('CRT','crt',s.crt||'1',[['1','1 · Simples Nacional'],['2','2 · Simples excesso'],['3','3 · Regime normal'],['4','4 · MEI']])+field('CNAE','cnae',s.cnae,'maxlength="7"')+field('Série fiscal','series',s.series)+field('Natureza da operação','operationNature',s.operationNature||'VENDA')+field('CSC ID','cscId',s.cscId,'autocomplete="off"')+field('Logradouro','street',a.street)+field('Número','number',a.number)+field('Complemento','complement',a.complement)+field('Bairro','district',a.district)+field('Código IBGE','cityCode',a.cityCode,'maxlength="7"')+field('Município','city',a.city)+field('UF','state',a.state,'maxlength="2"')+field('CEP','zip',a.zip,'maxlength="8"')+'<label class="fiscal-check"><input type="checkbox" name="autoIssue" '+(s.autoIssue?'checked':'')+' /><span>Emitir automaticamente após concluir a venda</span></label></div><div class="fiscal-actions"><button class="ops-primary" type="submit">Salvar dados fiscais</button></div></form>';}
 function profileForm(p={}){return '<form id="fiscal-profile-form" class="fiscal-form fiscal-subcard"><input type="hidden" name="id" value="'+esc(p.id||'')+'"><div class="fiscal-section-head"><div><h3>'+(p.id?'Editar perfil':'Novo perfil tributário')+'</h3></div></div><div class="fiscal-grid">'+field('Nome','name',p.name)+field('NCM','ncm',p.ncm,'maxlength="8"')+field('CEST','cest',p.cest,'maxlength="7"')+field('CFOP','cfop',p.cfop,'maxlength="4"')+select('Origem','origin',p.origin??'0',Array.from({length:9},(_,i)=>[String(i),String(i)]))+field('CSOSN','csosn',p.csosn,'maxlength="3"')+field('CST ICMS','icmsCst',p.icmsCst,'maxlength="3"')+field('CST PIS','pisCst',p.pisCst,'maxlength="2"')+field('CST COFINS','cofinsCst',p.cofinsCst,'maxlength="2"')+field('Unidade tributável','unit',p.unit||'UN','maxlength="6"')+field('CST IBS/CBS','ibsCbsCst',p.ibsCbsCst,'maxlength="3"')+field('cClassTrib','cClassTrib',p.cClassTrib,'maxlength="6"')+'<label class="fiscal-check"><input type="checkbox" name="active" '+(p.active!==false?'checked':'')+'><span>Perfil ativo</span></label></div><div class="fiscal-actions"><button class="ops-primary" type="submit">Salvar perfil</button></div></form>';}
 function profiles(){const rows=state.profiles.length?state.profiles.map(p=>'<button type="button" class="fiscal-list-item" data-profile="'+esc(p.id)+'"><span><strong>'+esc(p.name)+'</strong><small>NCM '+esc(p.ncm)+' · CFOP '+esc(p.cfop)+'</small></span><span class="ops-badge">'+(p.active?'ATIVO':'INATIVO')+'</span></button>').join(''):'<div class="fiscal-empty">Nenhum perfil tributário cadastrado.</div>';return '<section data-phase="P3"><div class="fiscal-section-head"><div><h3>P3 · Perfis tributários</h3><p>NCM, CFOP, ICMS/CSOSN, PIS/COFINS e RTC IBS/CBS.</p></div><button id="fiscal-profile-new" class="ops-secondary">Novo perfil</button></div><div class="fiscal-split"><div class="fiscal-list">'+rows+'</div><div id="fiscal-profile-editor">'+profileForm({})+'</div></div></section>';}
 function products(){const c=state.coverage||{totalProducts:state.products.length,configuredProducts:0,pendingProducts:state.products.length,coveragePercent:0};return '<section data-phase="P4"><div class="fiscal-section-head"><div><h3>P4 · Tributação por produto</h3><p>Vínculo produto → perfil fiscal e GTIN.</p></div><strong class="fiscal-coverage">'+esc(c.coveragePercent)+'%</strong></div><div class="fiscal-metrics"><div><strong>'+esc(c.totalProducts)+'</strong><span>Produtos</span></div><div><strong>'+esc(c.configuredProducts)+'</strong><span>Configurados</span></div><div><strong>'+esc(c.pendingProducts)+'</strong><span>Pendentes</span></div></div><form id="fiscal-product-form" class="fiscal-form fiscal-subcard"><div class="fiscal-grid"><label class="fiscal-field"><span>Produto</span><select name="productId" id="fiscal-product-select"><option value="">Selecione…</option>'+state.products.map(p=>'<option value="'+esc(p.id)+'">'+esc(p.name||p.id)+'</option>').join('')+'</select></label><label class="fiscal-field"><span>Perfil tributário</span><select name="profileId" id="fiscal-product-profile"><option value="">Selecione…</option>'+state.profiles.filter(p=>p.active!==false).map(p=>'<option value="'+esc(p.id)+'">'+esc(p.name)+'</option>').join('')+'</select></label>'+field('GTIN','gtin','')+'<div class="fiscal-readonly" id="fiscal-product-current"><span>Configuração atual</span><strong>Selecione um produto</strong></div></div><div class="fiscal-actions"><button class="ops-primary" type="submit">Salvar tributação do produto</button></div></form></section>';}
 function sequence(){const s=state.settings||{},q=state.sequence||{};return '<section data-phase="P5"><div class="fiscal-section-head"><div><h3>P5 · Sequência e prontidão</h3><p>Numeração fiscal e gate de produção.</p></div><span class="ops-badge">'+(state.readiness?.ready?'PRONTO':'BLOQUEADO')+'</span></div><form id="fiscal-sequence-form" class="fiscal-form fiscal-subcard"><div class="fiscal-grid">'+select('Documento','documentType',q.documentType||s.documentType||'nfce',[['nfce','NFC-e'],['nfe','NF-e']])+select('Ambiente','environment',q.environment||s.environment||'homologation',[['homologation','Homologação'],['production','Produção']])+field('Série','series',q.series||s.series)+field('Próximo número','nextNumber',q.nextNumber||1,'type="number" min="1" step="1"')+'</div><div class="fiscal-actions"><button class="ops-primary" type="submit">Salvar sequência</button></div></form><div class="fiscal-checklist">'+((state.readiness?.checks||[]).map(i=>'<div><span>'+(i.passed?'✓':'—')+'</span><p><strong>'+esc(i.label)+'</strong><small>'+esc(i.message||'')+'</small></p></div>').join('')||'<p class="ops-muted">Checklist indisponível.</p>')+'</div></section>';}
 function html(){const body=tab==='company'?company():tab==='profiles'?profiles():tab==='products'?products():sequence();return '<section id="fiscal-config-workspace" class="ops-card fiscal-workspace" data-phases="P2 P3 P4 P5"><div class="ops-card-head"><div><h2>Fiscal · Cadastros e Tributação</h2><p class="ops-muted">Configuração fiscal operacional completa; credenciais sensíveis permanecem protegidas.</p></div><button id="fiscal-config-refresh" class="ops-secondary">Atualizar</button></div><nav class="fiscal-tabs"><button data-tab="company" class="'+(tab==='company'?'active':'')+'">Empresa</button><button data-tab="profiles" class="'+(tab==='profiles'?'active':'')+'">Perfis</button><button data-tab="products" class="'+(tab==='products'?'active':'')+'">Produtos</button><button data-tab="sequence" class="'+(tab==='sequence'?'active':'')+'">Sequência</button></nav><div id="fiscal-config-body">'+body+'</div></section>';}
 const obj=f=>Object.fromEntries(new FormData(f).entries());
 async function refresh(){try{await load();render();}catch(e){toast(e.message,'error');}}
 function wire(s){s.querySelectorAll('[data-tab]').forEach(b=>b.onclick=()=>{tab=b.dataset.tab;render();});s.querySelector('#fiscal-config-refresh').onclick=refresh;
  s.querySelector('#fiscal-company-form')?.addEventListener('submit',async e=>{e.preventDefault();try{const d=obj(e.currentTarget);await api.saveSettings({provider:d.provider,documentType:d.documentType,environment:d.environment,autoIssue:e.currentTarget.elements.autoIssue.checked,cnpj:d.cnpj,stateRegistration:d.stateRegistration,legalName:d.legalName,tradeName:d.tradeName,crt:d.crt,cnae:d.cnae,series:d.series,operationNature:d.operationNature,cscId:d.cscId,address:{street:d.street,number:d.number,complement:d.complement,district:d.district,cityCode:d.cityCode,city:d.city,state:d.state,zip:d.zip}});toast('Dados fiscais salvos.','success');await refresh();}catch(x){toast(x.message,'error');}});
  const bindProfile=()=>s.querySelector('#fiscal-profile-form')?.addEventListener('submit',async e=>{e.preventDefault();try{const d=obj(e.currentTarget),body={name:d.name,ncm:d.ncm,cest:d.cest,cfop:d.cfop,origin:d.origin,csosn:d.csosn,icmsCst:d.icmsCst,pisCst:d.pisCst,cofinsCst:d.cofinsCst,unit:d.unit,ibsCbsCst:d.ibsCbsCst,cClassTrib:d.cClassTrib,active:e.currentTarget.elements.active.checked};d.id?await api.saveProfile(d.id,body):await api.createProfile(body);toast('Perfil tributário salvo.','success');await refresh();}catch(x){toast(x.message,'error');}});bindProfile();
  s.querySelector('#fiscal-profile-new')?.addEventListener('click',()=>{s.querySelector('#fiscal-profile-editor').innerHTML=profileForm({});bindProfile();});s.querySelectorAll('[data-profile]').forEach(b=>b.onclick=()=>{s.querySelector('#fiscal-profile-editor').innerHTML=profileForm(state.profiles.find(p=>String(p.id)===String(b.dataset.profile))||{});bindProfile();});
  s.querySelector('#fiscal-product-select')?.addEventListener('change',async e=>{const box=s.querySelector('#fiscal-product-current');if(!e.target.value){box.innerHTML='<span>Configuração atual</span><strong>Selecione um produto</strong>';return;}try{const x=await api.productFiscal(e.target.value);box.innerHTML='<span>Configuração atual</span><strong>'+esc(x?.name||'Sem perfil')+'</strong><small>'+esc(x?.gtin||'Sem GTIN')+'</small>';if(x?.profileId)s.querySelector('#fiscal-product-profile').value=x.profileId;}catch(x){toast(x.message,'error');}});
  s.querySelector('#fiscal-product-form')?.addEventListener('submit',async e=>{e.preventDefault();try{const d=obj(e.currentTarget);if(!d.productId||!d.profileId)throw new Error('Selecione produto e perfil tributário.');await api.saveProductFiscal(d.productId,{profileId:d.profileId,gtin:d.gtin||null,overrides:{}});toast('Tributação do produto salva.','success');await refresh();}catch(x){toast(x.message,'error');}});
  s.querySelector('#fiscal-sequence-form')?.addEventListener('submit',async e=>{e.preventDefault();try{const d=obj(e.currentTarget);await api.saveSequence({documentType:d.documentType,environment:d.environment,series:d.series,nextNumber:Number(d.nextNumber)});toast('Sequência fiscal salva.','success');await refresh();}catch(x){toast(x.message,'error');}});
 }
 function render(){if(!mounted?.isConnected)return;const old=mounted.querySelector('#fiscal-config-workspace'),h=document.createElement('div');h.innerHTML=html();const fresh=h.firstElementChild;if(old)old.replaceWith(fresh);else mounted.appendChild(fresh);wire(fresh);}
 async function mount(){const page=content.querySelector('.ops-page'),head=content.querySelector('.ops-head h1');if(!page||!head||head.textContent.trim()!=='Configurações')return;if(page===mounted&&page.querySelector('#fiscal-config-workspace'))return;mounted=page;await refresh();}
 new MutationObserver(()=>mount()).observe(content,{childList:true,subtree:true});mount();
})();
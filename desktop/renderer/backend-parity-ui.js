'use strict';

(() => {
  const ApiClient=window.PdvApiClient?.ApiClient;if(!ApiClient)return;
  const api=new ApiClient();
  const esc=value=>String(value??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
  const money=cents=>(Number(cents||0)/100).toLocaleString('pt-BR',{style:'currency',currency:'BRL'});
  const qty=value=>Number(value||0).toLocaleString('pt-BR',{maximumFractionDigits:3});
  const toast=(message,error=false)=>{const root=document.getElementById('toast-root');if(!root)return;const node=document.createElement('div');node.className=`toast ${error?'error':'success'}`;node.textContent=message;root.appendChild(node);setTimeout(()=>node.remove(),3200);};
  const currentPage=()=>document.getElementById('route-content')?.querySelector('.ops-page,.page');
  const title=()=>currentPage()?.querySelector('h1')?.textContent?.trim()||'';
  const field=(name,label,type='text',extra='')=>`<label class="field"><span>${esc(label)}</span><input name="${esc(name)}" type="${esc(type)}" ${extra}></label>`;

  async function mountInventory(){
    const page=currentPage();if(!page||title()!=='Estoque'||page.querySelector('#backend-parity-stock'))return;
    const heading=[...page.querySelectorAll('h2')].find(node=>node.textContent.trim()==='Posição de estoque');if(heading)heading.textContent='Posição do estoque principal (MAIN)';
    const card=document.createElement('section');card.id='backend-parity-stock';card.className='ops-card';
    card.innerHTML='<div class="ops-card-head"><div><h2>Estoque por local</h2><p class="ops-muted">Consulte e movimente um local específico sem misturar saldos entre lojas/depósitos.</p></div></div><div data-local-stock-body><div class="ops-loader"></div></div>';
    page.appendChild(card);
    try{
      const locations=await api.stockLocations();
      const cfg=await api.initialize();
      const body=card.querySelector('[data-local-stock-body]');
      body.innerHTML=`<div class="ops-form-grid"><label class="field"><span>Local</span><select id="parity-stock-location">${locations.map(row=>`<option value="${esc(row.id)}">${esc(row.name)} (${esc(row.id)})</option>`).join('')}</select></label><label class="field"><span>Visão</span><select id="parity-stock-view"><option value="local">Local selecionado</option><option value="aggregate">Total da empresa</option></select></label></div><div id="parity-stock-summary"></div><section class="ops-card"><h3>Movimentar local</h3><form id="parity-stock-move" class="ops-form-grid"><label class="field"><span>Produto</span><select name="productId" required></select></label><label class="field"><span>Tipo</span><select name="type"><option value="purchase">Entrada/compra</option><option value="adjustment-in">Ajuste de entrada</option><option value="adjustment-out">Ajuste de saída</option><option value="inventory-count">Inventário</option></select></label>${field('quantity','Quantidade','number','min="0.001" step="0.001" required')}${field('reason','Motivo','text','required')}<button class="ops-primary" type="submit">Registrar no local</button></form></section><div id="parity-stock-table"></div><small class="ops-muted">Terminal atual: ${esc(cfg.terminalId||'—')}</small>`;
      const locationSelect=body.querySelector('#parity-stock-location');const viewSelect=body.querySelector('#parity-stock-view');
      async function load(){
        const aggregate=viewSelect.value==='aggregate';const locationId=locationSelect.value||'MAIN';
        const [balances,movements]=await Promise.all([api.stockBalances({locationId,aggregate}),api.stockMovementsByLocation(aggregate?{}:{locationId})]);
        const products=balances.filter(row=>row.trackStock!==false);
        body.querySelector('#parity-stock-summary').innerHTML=`<p class="ops-muted">${aggregate?'Total consolidado da empresa':`Saldo físico em ${esc(locationId)}`}</p>`;
        body.querySelector('#parity-stock-table').innerHTML=`<div class="ops-table-wrap"><table class="ops-table"><thead><tr><th>Produto</th><th>Saldo</th><th>Mínimo</th><th>Situação</th></tr></thead><tbody>${balances.map(row=>`<tr><td>${esc(row.name)}</td><td>${qty(row.quantity)} ${esc(row.unit||'')}</td><td>${qty(row.minimumStock)}</td><td>${row.lowStock?'BAIXO':'OK'}</td></tr>`).join('')||'<tr><td colspan="4">Sem produtos.</td></tr>'}</tbody></table></div><details><summary>Movimentações deste local</summary><div class="ops-table-wrap"><table class="ops-table"><thead><tr><th>Produto</th><th>Tipo</th><th>Movimento</th><th>Depois</th></tr></thead><tbody>${movements.slice(-50).reverse().map(row=>`<tr><td>${esc(row.productId)}</td><td>${esc(row.type)}</td><td>${qty(row.quantityDelta)}</td><td>${qty(row.quantityAfter)}</td></tr>`).join('')||'<tr><td colspan="4">Sem movimentações.</td></tr>'}</tbody></table></div></details>`;
        const productSelect=body.querySelector('#parity-stock-move select[name="productId"]');productSelect.innerHTML=products.map(row=>`<option value="${esc(row.productId)}">${esc(row.name)}</option>`).join('');
        body.querySelector('#parity-stock-move button').disabled=aggregate;
      }
      locationSelect.addEventListener('change',()=>void load());viewSelect.addEventListener('change',()=>void load());
      body.querySelector('#parity-stock-move').addEventListener('submit',async event=>{event.preventDefault();const data=new FormData(event.currentTarget);const type=String(data.get('type'));const quantity=Number(data.get('quantity'));const movement={productId:data.get('productId'),locationId:locationSelect.value,type,reason:data.get('reason')};if(type==='inventory-count')movement.countedQuantity=quantity;else movement.quantityDelta=type==='adjustment-out'?-Math.abs(quantity):Math.abs(quantity);try{await api.saveInventoryMovement(movement);toast('Movimentação registrada no local.');event.currentTarget.reset();await load();}catch(error){toast(error.message,true);}});
      await load();
    }catch(error){card.querySelector('[data-local-stock-body]').innerHTML=`<div class="ops-error">${esc(error.message)}</div>`;}
  }

  async function mountLogistics(){
    const page=currentPage();if(!page||title()!=='Logística de estoque'||page.querySelector('#backend-terminal-stock'))return;
    const card=document.createElement('section');card.id='backend-terminal-stock';card.className='ops-card';card.innerHTML='<h2>Estoque por terminal</h2><p class="ops-muted">Defina de qual loja/depósito cada caixa baixa estoque. Vendas já abertas preservam o local original.</p><div data-terminal-stock-body><div class="ops-loader"></div></div>';page.appendChild(card);
    async function load(){try{const [locations,bindings]=await Promise.all([api.stockLocations(),api.terminalStockLocations()]);const body=card.querySelector('[data-terminal-stock-body]');body.innerHTML=`<form id="terminal-stock-form" class="ops-form-grid"><label class="field"><span>Terminal</span><select name="terminalId">${bindings.map(row=>`<option value="${esc(row.terminalId)}">${esc(row.name||row.terminalId)} (${esc(row.terminalId)})</option>`).join('')}</select></label><label class="field"><span>Local de estoque</span><select name="locationId">${locations.map(row=>`<option value="${esc(row.id)}">${esc(row.name)} (${esc(row.id)})</option>`).join('')}</select></label><button class="ops-primary">Vincular terminal</button></form><div class="ops-table-wrap"><table class="ops-table"><thead><tr><th>Terminal</th><th>Local</th><th>Origem</th></tr></thead><tbody>${bindings.map(row=>`<tr><td>${esc(row.name||row.terminalId)}</td><td>${esc(row.locationName||row.locationId)} (${esc(row.locationId)})</td><td>${row.fallback?'Padrão MAIN':'Vínculo explícito'}</td></tr>`).join('')||'<tr><td colspan="3">Nenhum terminal cadastrado.</td></tr>'}</tbody></table></div>`;body.querySelector('#terminal-stock-form')?.addEventListener('submit',async event=>{event.preventDefault();const data=new FormData(event.currentTarget);try{await api.bindTerminalStockLocation(data.get('terminalId'),data.get('locationId'));toast('Local do terminal atualizado.');await load();}catch(error){toast(error.message,true);}});}catch(error){card.querySelector('[data-terminal-stock-body]').innerHTML=`<div class="ops-error">${esc(error.message)}</div>`;}}
    await load();
  }

  async function mountReturns(){
    const page=currentPage();if(!page||title()!=='Devolução'||page.querySelector('#backend-return-cancel'))return;
    const card=document.createElement('section');card.id='backend-return-cancel';card.className='ops-card';card.innerHTML='<h2>Gerenciar devoluções</h2><p class="ops-muted">Cancelamento exige motivo e reverte os efeitos da devolução no estoque/caixa.</p><div data-return-manage></div>';page.appendChild(card);
    async function load(){try{const rows=await api.returns();card.querySelector('[data-return-manage]').innerHTML=`<div class="ops-table-wrap"><table class="ops-table"><thead><tr><th>ID</th><th>Venda</th><th>Total</th><th>Status</th><th></th></tr></thead><tbody>${rows.map(row=>`<tr><td>${esc(row.id)}</td><td>${esc(row.saleId)}</td><td>${money(row.totalCents)}</td><td>${esc(row.status)}</td><td>${row.status==='COMPLETED'?`<button class="ops-link danger" data-cancel-return="${esc(row.id)}">Cancelar devolução</button>`:''}</td></tr>`).join('')||'<tr><td colspan="5">Nenhuma devolução registrada.</td></tr>'}</tbody></table></div>`;card.querySelectorAll('[data-cancel-return]').forEach(button=>button.addEventListener('click',async()=>{const reason=window.prompt('Motivo do cancelamento da devolução:');if(!reason)return;try{await api.cancelReturn(button.dataset.cancelReturn,reason);toast('Devolução cancelada.');await load();}catch(error){toast(error.message,true);}}));}catch(error){card.querySelector('[data-return-manage]').innerHTML=`<div class="ops-error">${esc(error.message)}</div>`;}}
    await load();
  }

  async function mountFinance(){
    const page=currentPage();if(!page||title()!=='Financeiro'||page.querySelector('#backend-finance-parity'))return;
    const card=document.createElement('section');card.id='backend-finance-parity';card.className='ops-card';card.innerHTML='<div class="ops-grid two"><section><h2>Contas financeiras</h2><form id="parity-account-form" class="ops-form-grid">'+field('name','Nome','text','required')+'<label class="field"><span>Tipo</span><select name="type"><option value="CASH">Dinheiro</option><option value="BANK">Banco</option><option value="CARD">Cartão</option><option value="OTHER">Outro</option></select></label><button class="ops-primary">Criar conta</button></form><div data-account-list></div></section><section><h2>Estornar baixas</h2><p class="ops-muted">Baixas financeiras podem ser estornadas sem apagar o histórico.</p><div data-settlement-list></div></section></div>';page.appendChild(card);
    async function load(){try{const [accounts,entries]=await Promise.all([api.financeAccounts(true),api.financeEntries()]);card.querySelector('[data-account-list]').innerHTML=accounts.map(row=>`<div class="ops-row"><span>${esc(row.name)}</span><small>${esc(row.type)} · ${row.active?'ATIVA':'INATIVA'}</small></div>`).join('')||'<p class="ops-muted">Nenhuma conta.</p>';const settlements=entries.flatMap(entry=>(entry.settlements||[]).filter(s=>!s.reversedAt).map(s=>({...s,entryDescription:entry.description})));card.querySelector('[data-settlement-list]').innerHTML=settlements.map(row=>`<div class="ops-row"><div><strong>${esc(row.entryDescription)}</strong><small>${money(row.amountCents)} · ${esc(row.method||'manual')}</small></div><button class="ops-link danger" data-reverse-settlement="${esc(row.id)}">Estornar</button></div>`).join('')||'<p class="ops-muted">Nenhuma baixa ativa.</p>';card.querySelectorAll('[data-reverse-settlement]').forEach(button=>button.addEventListener('click',async()=>{const reason=window.prompt('Motivo do estorno:');if(!reason)return;try{await api.reverseFinanceSettlement(button.dataset.reverseSettlement,reason);toast('Baixa estornada.');await load();}catch(error){toast(error.message,true);}}));}catch(error){toast(error.message,true);}}
    card.querySelector('#parity-account-form').addEventListener('submit',async event=>{event.preventDefault();const data=new FormData(event.currentTarget);try{await api.createFinanceAccount({name:data.get('name'),type:data.get('type')});toast('Conta financeira criada.');event.currentTarget.reset();await load();}catch(error){toast(error.message,true);}});await load();
  }

  async function mountServices(){
    const page=currentPage();if(!page||title()!=='Serviços'||page.querySelector('#backend-services-lifecycle'))return;
    const card=document.createElement('section');card.id='backend-services-lifecycle';card.className='data-card';card.innerHTML=`<h2>Ciclo do atendimento</h2><p class="vertical-rule">Use o ID retornado ao agendar para avançar o atendimento e gerar a venda canônica.</p><form id="services-lifecycle-form" class="vertical-form">${field('appointmentId','ID do atendimento','text','required')}<label class="field"><span>Ação</span><select name="action"><option value="IN_PROGRESS">Iniciar</option><option value="COMPLETED">Concluir</option><option value="CANCELLED">Cancelar</option><option value="NO_SHOW">Não compareceu</option><option value="SALE">Gerar venda</option></select></label><label class="field"><span>Operador da venda</span><select name="operatorId"></select></label><button class="primary-button">Executar</button></form>`;page.appendChild(card);
    try{const [users,cfg]=await Promise.all([api.users(true),api.initialize()]);const select=card.querySelector('select[name="operatorId"]');select.innerHTML=users.filter(u=>u.active!==false).map(u=>`<option value="${esc(u.id)}">${esc(u.name)} · ${esc(u.role)}</option>`).join('');card.querySelector('#services-lifecycle-form').addEventListener('submit',async event=>{event.preventDefault();const data=new FormData(event.currentTarget);const id=String(data.get('appointmentId'));const action=data.get('action');try{if(action==='SALE')await api.request(`/api/v1/vertical/services/appointments/${encodeURIComponent(id)}/sale`,{method:'POST',body:{terminalId:cfg.terminalId,operatorId:data.get('operatorId')}});else await api.request(`/api/v1/vertical/services/appointments/${encodeURIComponent(id)}/status`,{method:'PATCH',body:{status:action}});toast(action==='SALE'?'Venda do atendimento criada.':'Atendimento atualizado.');}catch(error){toast(error.message,true);}});}catch(error){toast(error.message,true);}
  }

  async function mountWorkshop(){
    const page=currentPage();if(!page||title()!=='Oficina'||page.querySelector('#backend-workshop-lifecycle'))return;
    const card=document.createElement('section');card.id='backend-workshop-lifecycle';card.className='data-card';card.innerHTML=`<h2>Operação completa da OS</h2><form id="work-item-form" class="vertical-form">${field('orderId','ID OS','text','required')}<label class="field"><span>Item</span><select name="kind"><option value="PART">Peça/produto</option><option value="LABOR">Mão de obra/serviço</option></select></label>${field('referenceId','ID produto/serviço','text','required')}${field('quantity','Quantidade','number','min="0.001" step="0.001" value="1"')}<button>Adicionar item</button></form><form id="work-status-form" class="vertical-form">${field('orderId','ID OS','text','required')}<label class="field"><span>Etapa</span><select name="status"><option value="DIAGNOSIS">Diagnóstico</option><option value="QUOTED">Orçamento</option><option value="APPROVED">Aprovação</option><option value="IN_PROGRESS">Em execução</option><option value="READY">Pronta</option><option value="SALE">Gerar venda/fechar</option><option value="CANCELLED">Cancelar</option></select></label>${field('note','Diagnóstico / aprovação / motivo','text')}<label class="field"><span>Operador da venda</span><select name="operatorId"></select></label><button class="primary-button">Avançar OS</button></form><pre id="work-parity-output" class="vertical-output"></pre>`;page.appendChild(card);
    try{const [users,cfg]=await Promise.all([api.users(true),api.initialize()]);const operator=card.querySelector('select[name="operatorId"]');operator.innerHTML=users.filter(u=>u.active!==false).map(u=>`<option value="${esc(u.id)}">${esc(u.name)} · ${esc(u.role)}</option>`).join('');card.querySelector('#work-item-form').addEventListener('submit',async event=>{event.preventDefault();const data=new FormData(event.currentTarget);const kind=data.get('kind');const body={kind,quantity:Number(data.get('quantity'))};if(kind==='PART')body.productId=data.get('referenceId');else body.serviceId=data.get('referenceId');try{const order=await api.request(`/api/v1/vertical/workshop/orders/${encodeURIComponent(data.get('orderId'))}/items`,{method:'POST',body});card.querySelector('#work-parity-output').textContent=`Status: ${order.status}\nItens: ${order.items.length}\nTotal: ${money(order.totalCents)}`;toast('Item adicionado à OS.');}catch(error){toast(error.message,true);}});card.querySelector('#work-status-form').addEventListener('submit',async event=>{event.preventDefault();const data=new FormData(event.currentTarget);const id=data.get('orderId');const status=data.get('status');const note=String(data.get('note')||'');try{let order;if(status==='SALE')order=await api.request(`/api/v1/vertical/workshop/orders/${encodeURIComponent(id)}/sale`,{method:'POST',body:{terminalId:cfg.terminalId,operatorId:data.get('operatorId')}});else if(status==='CANCELLED')order=await api.request(`/api/v1/vertical/workshop/orders/${encodeURIComponent(id)}/cancel`,{method:'POST',body:{reason:note}});else{const body={status};if(status==='DIAGNOSIS')body.diagnosis=note;if(status==='APPROVED')body.approvalNote=note;order=await api.request(`/api/v1/vertical/workshop/orders/${encodeURIComponent(id)}/status`,{method:'PATCH',body});}const current=status==='SALE'?await api.request(`/api/v1/vertical/workshop/orders/${encodeURIComponent(id)}`):order;card.querySelector('#work-parity-output').textContent=`Status: ${current.status}\nItens: ${current.items?.length||0}\nTotal: ${money(current.totalCents||order.totalCents)}\nVenda: ${current.saleId||order.id||'-'}`;toast(status==='SALE'?'OS convertida em venda.':'OS atualizada.');}catch(error){toast(error.message,true);}});}catch(error){toast(error.message,true);}
  }

  function mount(){void mountInventory();void mountLogistics();void mountReturns();void mountFinance();void mountServices();void mountWorkshop();}
  const root=document.getElementById('route-content')||document.body;new MutationObserver(()=>queueMicrotask(mount)).observe(root,{childList:true,subtree:true});mount();
})();

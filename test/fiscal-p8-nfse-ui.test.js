'use strict';
const test=require('node:test');const assert=require('node:assert/strict');const fs=require('node:fs');const path=require('node:path');
const root=path.resolve(__dirname,'..');const read=p=>fs.readFileSync(path.join(root,p),'utf8');
test('P8 NFS-e has an independent customer/admin UI surface',()=>{const html=read('desktop/renderer/index.html');const ui=read('desktop/renderer/nfse-ui.js');const client=read('desktop/renderer/nfse-api-client.js');assert.match(html,/nfse-api-client\.js/);assert.match(html,/nfse-ui\.js/);for(const marker of ['nfse-workspace','nfse-issue-form','data-nfse-detail','data-nfse-process','data-nfse-reconcile','data-nfse-retry','data-nfse-event'])assert.match(ui,new RegExp(marker));for(const marker of ['documents(status','requestIssue(body)','process(id)','reconcile(id)','retry(id)','events(id)','registerEvent(id'])assert.ok(client.includes(marker),marker);});
test('P8 retry endpoint reuses the existing NFS-e document lifecycle',()=>{const router=read('server/nfse-router.js');const service=read('js/domains/nfse/nfse-service.js');assert.match(router,/\/retry/);assert.match(router,/runtime\.nfse\.retryIssue/);assert.match(service,/UNKNOWN exige reconciliacao antes de retry/);assert.match(service,/status='PENDING'/);});
test('P8 QA flow covers NFS-e list and issue surface',()=>{const qa=JSON.parse(read('qa/artisys-qa.config.json'));assert.equal(qa.flows['fiscal-nfse-p8'],'flows/fiscal-nfse-p8.json');assert.ok(qa.qaProfiles.release.flows.includes('fiscal-nfse-p8'));});

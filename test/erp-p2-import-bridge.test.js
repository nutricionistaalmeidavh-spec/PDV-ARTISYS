'use strict';
const test=require('node:test');const assert=require('node:assert/strict');const fs=require('node:fs');const os=require('node:os');const path=require('node:path');const {createImportPicker}=require('../desktop/import-bridge.cjs');
test('safe import picker accepts OFX as UTF-8 while preserving file controls',async()=>{const dir=fs.mkdtempSync(path.join(os.tmpdir(),'pdv-ofx-picker-'));try{const file=path.join(dir,'extrato.ofx');fs.writeFileSync(file,'<OFX>á</OFX>','utf8');let options=null;const pick=createImportPicker({dialog:{showOpenDialog:async(_parent,input)=>{options=input;return{canceled:false,filePaths:[file]};}}});const result=await pick();assert.equal(result.format,'ofx');assert.equal(result.content,'<OFX>á</OFX>');assert.ok(options.filters[0].extensions.includes('csv'));assert.ok(options.filters[0].extensions.includes('xlsx'));assert.ok(options.filters[0].extensions.includes('ofx'));}finally{fs.rmSync(dir,{recursive:true,force:true});}});
